{
  "type": "module",
  "scripts": {
    "clean": "rm -rf build tmp",
    "build": "RACK_ENV=production bundle exec middleman build",
    "start": "RACK_ENV=development bundle exec middleman server",
    "format-flake": "nix fmt flake.nix",
    "format-ruby": "bundle exec rufo ."
  },
  "dependencies": {
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "@tailwindcss/typography": "^0.5.19",
    "postcss": "^8.5.14",
    "vite": "^8.0.11",
    "vite-plugin-full-reload": "^1.2.0",
    "vite-plugin-ruby": "^5.2.2"
  },
  "overrides": {
    "vite-plugin-full-reload": {
      "picocolors": "^1.1.1",
      "picomatch": "^4.0.4"
    }
  }
}
